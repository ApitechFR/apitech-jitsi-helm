{{- if and .Values.excalidraw.frontend.enabled (not .Values.excalidraw.useExternalExcalidraw) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "jitsi-meet.excalidraw.frontend.fullname" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "jitsi-meet.excalidraw.frontend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "jitsi-meet.excalidraw.frontend.selectorLabels" . | nindent 8 }}
    spec:
    {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
    {{- end }}    
      containers:
      - name: excalidraw
        securityContext:
          {{- toYaml .Values.excalidraw.frontend.securityContext | nindent 12 }}
        image: "{{ .Values.excalidraw.frontend.image.repository }}:{{ .Values.excalidraw.frontend.image.tag }}"
        resources:
          {{- toYaml .Values.excalidraw.frontend.resources | nindent 12 }}
{{- end }}