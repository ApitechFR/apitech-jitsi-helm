{{- if .Values.dashboard.grafana.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "jitsi-meet.dashboard.grafana.fullname" . }}
  labels:
    {{- include "jitsi-meet.dashboard.grafana.labels" . | nindent 4 }}
{{- end }}
